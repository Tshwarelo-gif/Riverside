{
  "buildCommand": "cd ../.. && npm install && npm run build:shared && npm run build --workspace=apps/web",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
